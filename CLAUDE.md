# Agent-instruction compatibility

@AGENTS.md

The canonical repository instructions are in `AGENTS.md`, and the canonical
open-format skills are in `.agents/skills/`. The `.claude/skills` path is a
compatibility symlink. Detailed architecture lives in `docs/architecture.md`.
